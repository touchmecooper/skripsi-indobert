# ---------------------------------------------------------------------------
# PENTING: baris '--extra-index-url' di bawah WAJIB ada sebelum baris torch.
# Ini memaksa pip mengambil build CPU-ONLY dari PyTorch (bukan build GPU/CUDA
# default dari PyPI yang ukurannya jauh lebih besar & butuh RAM lebih banyak).
# Tanpa ini, deploy di hosting gratis (RAM terbatas) berisiko gagal/OOM.
# ---------------------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cpu

flask==3.0.3
flask-sqlalchemy==3.1.1
pypdf==5.9.0
torch==2.3.1+cpu
transformers==4.44.2
huggingface_hub==0.24.6
accelerate==0.33.0
gunicorn==22.0.0
